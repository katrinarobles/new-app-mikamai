<div class="container">
  <div class="row justify-content-center">
    <div class="col-6 m-2">
      <h1> Welcome <%= current_user.surname.split(" ").first.capitalize %>,</h1>
      <!-- <h1>Pages#home</h1> -->
      <p> This is your dashboard</p>
      <p><%= cl_image_tag current_user.photo.key, height: 300, width: 400, crop: :fill %>
      <p>Name: <%= current_user.surname.split(" ").first.capitalize %></p>
      <p>Field: <%= current_user.field_name.split(" ").first.capitalize %></p>
      <p>Email: <%= current_user.email %></p>

    </div>
  </div>
</div>
